---
title: v0.2.0-rc1
---

See also the [v0.1.x to v0.2.0 upgrade guide](/docs/upgrading-from-v0.1.x-to-v0.2.0).

### BREAKING CHANGES

#### address-codec

- `DecodeXAddress` and `XAddressToClassicAddress` now return `hasTag`, preserving explicit tag `0` separately from no tag.

#### binary-codec

- `AccountID.FromJSON` now rejects X-addresses that carry an embedded tag (returning `ErrAccountIDTagNotAllowed`). Previously the tag was silently dropped for non-`Account`/`Destination` AccountID fields, including nested `SignerEntry.Account` and `EncodeForMultisigning`.
- `Amount` serialization no longer accepts `float64` values. Use strings, `json.Number`, or exact amount types to preserve precision.
- Removed the exported `ErrUInt64OutOfRange` error variable. `UInt64.FromJSON` now returns `ErrInvalidUInt64String` for all invalid inputs.
- Changed the exported `MaxDrops` constant to a typed `uint64` drops limit.
- Removed the exported `MinXRP` constant. Native XRP amount serialization validates drops, not XRP-denominated decimal values.
- `UInt64.FromJSON` now accepts only 1 to 16 character hex strings. Decimal-looking inputs are parsed as hex, so `"10"` is `0x10`, not decimal `10`.
- X-address encoding now rejects duplicate `SourceTag`/`DestinationTag` whenever the X-address carries an embedded tag, including explicit tag `0`.

#### keypairs

- `GenerateSeed` now accepts caller-supplied entropy as `[]byte` instead of `string`. Empty or nil entropy still generates random entropy with the provided randomizer, while non-empty entropy must be exactly 16 raw bytes.

#### xrpl

- `SortSigners` now returns an error when signer extraction or address decoding fails.

#### xrpl/transaction

- All loan transaction `Flatten()` methods now return `FlatTransaction` instead of `map[string]any`.
- Removed exported `DomainIDLength` and `SHA512HalfLength` constants. Use `Hex256Length`, `IsHex256`, `IsDomainID`, or `IsLedgerEntryID`.

#### xrpl/wallet

- Renamed `ErrAddressTagNotZero` to `ErrAddressHasTag`. The error now fires for any embedded X-address tag, including explicit tag `0`.

### Added

#### binary-codec

- Added `ErrDuplicateXAddressTag` and `ErrAccountIDTagNotAllowed` sentinels for tagged X-address validation.

#### keypairs

- Exported `ErrRandomizerRequired` and `ErrInvalidEntropyLength` for `GenerateSeed` error handling.

#### pkg/typecheck

- Added `IsHexBlob` for hex-encoded whole-byte validation.

#### xrpl

- Added `SortByAccountID` for canonical account ID byte ordering.
- Added multisigned payment integration coverage and expanded lending protocol integration coverage.

#### xrpl/queries/account

- Added `LoanObject` and `LoanBrokerObject` constants for `account_objects` queries.

#### xrpl/rpc

- Added `GetXrpBalanceValidated`, `GetXrpDropsBalanceValidated`, and `SetLogger`.

#### xrpl/testutil

- Added `GetLedgerEntry`, `GetXrpBalanceValidated`, `GetXrpDropsBalanceValidated`, and `AutofillMultisigned` to the integration `Client` interface.

#### xrpl/transaction

- Exported `MinTransferRate` and `MaxTransferRate`.

#### xrpl/transaction/types

- Added `IsZero() bool` to the `CurrencyAmount` interface and renamed the old `IssuedCurrencyAmount.IsZero` empty-object check to `IsEmpty`.

#### xrpl/websocket

- Added `GetXrpBalanceValidated`, `GetXrpDropsBalanceValidated`, and `SetLogger`.

### Changed

#### docs

- Added a v0.1.x to v0.2.0 upgrade guide and v0.2.x changelog docs for the release candidate.
- Added wallet credential leakage warnings to wallet docs and examples.

#### pkg/crypto

- Ed25519 and SECP256K1 `Sign` now wrap hex decode failures with `ErrInvalidPrivateKey`.

#### pkg/decodehook

- Replaced the archived `github.com/mitchellh/mapstructure` dependency with `github.com/go-viper/mapstructure/v2`.

#### xrpl/currency

- Native amount conversion helpers now use exact rational arithmetic internally instead of `float64`.
- `DropsPerXrp` is deprecated but remains available for compatibility.

#### xrpl/rpc

- `FundWallet` now polls the validated ledger after faucet funding.
- HTTP 503 retries now recreate requests, close retry response bodies, and scope `cfg.timeout` to a single attempt.

#### xrpl/transaction

- `NFTokenCreateOffer.Validate` now reports amount and NFTokenID errors before owner, destination, and flag errors.

#### xrpl/websocket

- `FundWallet` now polls the validated ledger after faucet funding.
- `Connect` must not be called synchronously from stream or error handlers.
- `OnXxx` handler registration now atomically replaces the previous handler for the same stream.
- `Request` maps connection-layer `ErrNotConnected` back to public `ErrNotConnectedToServer`.

### Fixed

#### address-codec

- Hardened Base58Check, classic-address, seed, and X-address decoding validation.

#### binary-codec

- Fixed caller-map mutation in `Encode`, `EncodeForSigning`, and `EncodeForMultisigning`.
- Fixed `FieldIDCodec.Decode` stdout output.
- Fixed XRP amount precision, IOU canonical range checks, UInt64 hex handling, variable-length prefix encoding at length 12480, PathSet malformed input handling, and XChainBridge malformed input handling.

#### keypairs

- `GenerateSeed` now validates entropy length and returns errors instead of silently truncating or panicking.
- Keypair signing and validation now reject keys shorter than the crypto prefix.

#### pkg/crypto

- Ed25519 signing and validation now check malformed key and signature lengths before slicing or verifying.

#### xrpl

- Multisign helpers now propagate signer sort errors, use canonical account ID byte ordering, reject mismatched blobs, reject invalid signer signatures, and return `ErrInvalidSigner` for malformed signer data.

#### xrpl/currency

- XRP drops conversions now preserve precision up to the maximum XRP supply and reject overly large parser inputs.

#### xrpl/rpc

- Batch inner transaction autofill now validates inner accounts and `NetworkID` values before filling syntactically invalid transactions.
- Counterparty signer lookup now uses the current ledger for pre-validation availability.
- Remote non-TLS URL warnings now handle bare hosts and redact URL userinfo.
- RPC response bodies are capped at 64 MiB by default. Use `WithMaxResponseSize(0)` to disable the limit.

#### xrpl/transaction

- Added local validation for AccountSet flags and transfer rates, SignerListSet signer entries, Escrow condition and fulfillment blobs, nil amount flattening, zero amounts, and NFToken IDs.

#### xrpl/wallet

- `Sign` and `Multisign` now reject nil transaction maps with `ErrNilTransaction` and no longer mutate caller-provided transaction maps while signing.

#### xrpl/websocket

- Batch inner transaction autofill now validates inner accounts and `NetworkID` values before filling syntactically invalid transactions.
- Remote non-TLS URL warnings now fire once per client, handle bare hosts, and redact URL userinfo.
- WebSocket inbound messages are capped at 16 MiB by default. Use `WithMaxResponseSize(0)` to disable the limit.
- WebSocket request dispatch, concurrent reads, reconnect budgeting, reconnect backoff, stream handler lifecycle, and stale handler dispatch were hardened to avoid request blocking, reconnect races, and handler leaks.
