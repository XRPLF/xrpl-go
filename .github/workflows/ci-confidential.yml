name: Confidential CI

on:
  push:
    branches: [main]
    paths: &paths
      - 'confidential/**'
      - '!confidential/**/*.md'
      - 'confidential/**/testdata/**'
      - 'pkg/mptsizes/**'
      - '!pkg/mptsizes/**/*.md'
      - 'pkg/mptsizes/**/testdata/**'
      - 'Makefile'
      - '.golangci.yml'
      - '.ci-config/**'
      - 'scripts/localnet-integration.sh'
      - 'scripts/summarize-go-test-report.sh'
      - 'scripts/report-api-compatibility.sh'
      - '.github/workflows/ci-confidential.yml'
      - '.github/workflows/ci-tests.yml'
      - '.github/workflows/ci-checks.yml'
      - '.github/workflows/ci-api-compatibility.yml'
      - '.github/workflows/ci-cgo-matrix.yml'
      - '.github/workflows/update-mpt-crypto.yml'
  pull_request:
    branches: ['**']
    # API acknowledgements can change with the PR title or description.
    types: [opened, synchronize, reopened, edited]
    paths: *paths
  schedule:
    # Scan for newly disclosed CVEs even when no code changes.
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  # Keep metadata-only PR edits from canceling full CI runs.
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}-${{ github.event.action == 'edited' && 'metadata' || 'full' }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  tests:
    if: github.event_name != 'schedule' && github.event.action != 'edited'
    uses: ./.github/workflows/ci-tests.yml
    with:
      module: confidential

  native-tests:
    if: github.event_name != 'schedule' && github.event.action != 'edited'
    uses: ./.github/workflows/ci-cgo-matrix.yml

  checks:
    if: github.event.action != 'edited'
    uses: ./.github/workflows/ci-checks.yml
    with:
      module: confidential

  api-compatibility:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/ci-api-compatibility.yml
    with:
      module: confidential
